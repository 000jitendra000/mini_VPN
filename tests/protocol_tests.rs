//! Placeholder tests for the wire protocol / packet framing.
//!
//! There is no protocol yet -- framing is introduced in v0.5. This file
//! exists so protocol tests have a home once `src/protocol.rs` is
//! implemented.

#[test]
fn placeholder() {
    assert!(true);
}